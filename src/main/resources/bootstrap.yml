spring:
  application:    name: computer-vision-poc

server:  port: ${SERVER_PORT:8082}          
  
management:  endpoints:    web:      exposure:        include: 'info, health, metrics, httptrace, loggers, prometheus, beans'        